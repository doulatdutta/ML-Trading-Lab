# Scripts

Future command-line entry points belong here, for example historical import, feature generation, model training, walk-forward validation, and report generation. Keep scripts thin; reusable logic belongs under `src/ml_trading_lab`.
